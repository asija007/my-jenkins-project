pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'No build needed for static HTML'
            }
        }

        stage('Test') {
            steps {
                echo 'No tests for static HTML'
            }
        }

        stage('Deploy') {
            steps {
                echo 'You can copy these files to a server or a folder'
            }
        }
    }
}
